# Architecture Diagrams

`architecture-v3.png` — SIFTGuard system architecture, v3.

Identifies the architectural pattern (Custom MCP Server + Direct Agent Extension)
and distinguishes prompt-based guardrails (defense-in-depth) from architectural
guardrails (enforced by code) per SANS Find Evil hackathon submission rule #3.
